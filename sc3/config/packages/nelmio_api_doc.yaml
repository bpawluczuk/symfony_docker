nelmio_api_doc:
    documentation:
        info:
            title: SC3
            description: Sales Companion 3.0
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
